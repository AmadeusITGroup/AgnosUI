const t=`export const toSlotContextWidget = (w) => ({
    actions: w.actions,
    api: w.api,
    directives: w.directives,
    state$: w.state$,
    stores: w.stores,
});
export const INVALID_VALUE = Symbol();
`;export{t as default};
