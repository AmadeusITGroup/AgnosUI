const n=`/**
 * A noop function
 */
export const noop = () => { };
/**
 * The identity function
 *
 * @param x - the arg
 * @returns the arg
 */
export const identity = (x) => x;
`;export{n as default};
